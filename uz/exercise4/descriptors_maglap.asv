function [ D ] = descriptors_maglap( I, px, py, m, sigma, bins)
    rad = round((m - 1) / 2);
    [h, w] = size(I);
    [Imag, Ilap] = features_maglap(I, sigma, bins);
    for i = 1:length(px)
        minx
        maxx
        
    end

end

